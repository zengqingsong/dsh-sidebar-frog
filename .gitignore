node_modules/
.DS_Store
*.log
/docs

# Local launcher for this machine's `dsh web` service. It force-kills whatever
# listens on port 3080 and restarts the service in this directory, so it is a
# footgun in somebody else's checkout — keep it out of the published tree.
/start-dsh.bat
